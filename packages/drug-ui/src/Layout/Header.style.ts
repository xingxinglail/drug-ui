import { Style, Styles } from 'jss';

const root: Style = {
    height: 64,
    lineHeight: 64,
    padding: [0, 50],
    backgroundColor: '#001529'
};

export const styles = (): Styles => ({
    root
});
